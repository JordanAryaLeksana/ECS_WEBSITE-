
import React from 'react'
import CardProps from '@/types/Card'
import clsxm from '@/lib/clsxm'

const Card = ({}:CardProps) : React.ReactElement => {
  return (
    <div className={clsxm([
        ""
    ])}>

    </div>
  )
}

export default Card